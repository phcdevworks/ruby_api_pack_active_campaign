# TODO

No active work is currently open in this gem. Phases 1 through 5 are
complete — see [ROADMAP.md](ROADMAP.md) for the full delivery history and
[CHANGELOG.md](CHANGELOG.md) for release-by-release detail.

New work here remains demand-driven: it opens only when a concrete consumer
need surfaces one of the candidates listed in ROADMAP.md's "What's Next"
(Rails initializer/payload examples, pagination and rate-limit guidance, a
typed API error class, request timeout configuration).

When a new phase opens, add it here with the same checklist structure the
completed phases used in `ROADMAP.md`.
